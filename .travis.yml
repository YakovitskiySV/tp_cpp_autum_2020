dist: trusty
sudo: required
language: c

os: linux
compiler: gcc

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-9
      - gcc-9
      - valgrind
      - cmake
      - cppcheck
      - lcov
      - gcovr

before_script:
  - sudo apt-get install -y libgtest-dev
  - sudo apt-get upgrade -y
  - cd /usr/src/gtest
  - sudo cmake .
  - sudo make
  - sudo cp *.a /usr/lib

script:
  - cd "${TRAVIS_BUILD_DIR}"
  - ./linters/run.sh
  - CXX=/usr/bin/g++-9 CC=/usr/bin/gcc-9
  - cmake .
  - make
  - valgrind --version
  - valgrind -v --show-leak-kinds=all --track-origins=yes --leak-check=full ./tests.out
  - bash <(curl -s https://codecov.io/bash)
